# Work Checkpoints

> Concise, dated session checkpoints (newest first). High-signal only — durable
> facts, decisions, current state, next steps. Not a spec (see `ROADMAP.md` /
> `AI_CONTEXT.md`). Generated by the `/checkpoint` command.

## 2026-06-24 — Pivot to code-server web IDE; layout rework (Navigator right)

**Decision:** full IDE that edits the project = a *server* (no in-browser option);
chose **code-server** (VS Code in browser, MIT) over JupyterLab/Pyodide. Language
stays **Python** (ML/quant; native libs are fast). Pyodide in-browser IDE (a
parallel session's PR #17) is now unwired/dead code in the UI repo.

**Shipped:**
- **VM (main):** `jupyterlab` **removed**, **`code-server`** added (localhost:8080
  via SSH tunnel; `CODE_SERVER_PASSWORD` in deploy/.env; volume `jupyter_work`→`workspace`).
  Backends' `JUPYTER_*` env dropped.
- **UI (dev):** layout reworked — **Navigator moved to the RIGHT**, **code-server
  IDE on the LEFT** (collapsible/resizable, "Open in tab" fallback), chart center.
  The `+` buttons are unwired → placeholder ("link & run a Python function" later).
- **Backend (dev):** removed Jupyter notebook-API (`jupyter_client`, `/custom/create`,
  `/notebooks`). Sandbox custom-indicator compute/list unchanged.
- Earlier this session: **#3 (render custom indicators on chart) → prod**; Phase 2
  Jupyter-seeding built then removed on the pivot.

**State:** prod = #3 loop + old left-nav layout; dev = new layout + Jupyter gone;
VM runs code-server. **Next:** user sets `CODE_SERVER_PASSWORD` + tunnels to use it;
the iframe embed fights https↔http-localhost (local UI smoothest, or add code-server
framing config); rebuild the "run code → chart" exec path when wiring IDE→chart.

## 2026-06-22 — Custom-indicator IDE loop live in prod; context system + infra CD

**Shipped to prod (all repos):**
- **AI context system**: `docs/ARCHITECTURE.md`, `AI_ONBOARDING.md`, `ROADMAP.md`,
  per-repo `AI_CONTEXT.md`, `CLAUDE.md` START-HERE block.
- **Chart ranges** 5D/3M/6M/YTD (was 1W); backend `RANGE_MAP`/`IND_CFG` + UI tabs.
- **Navigator sidebar**: Indicators (Overlays/Oscillators) · Strategies · Saved
  Views; per-section search + `+` buttons. Collapsible top/bottom panels. Floating
  widget removed.
- **Custom-indicator IDE (Phase 1b + #3) — full loop in prod:** author in
  JupyterLab → `tsp.publish()` → registry → backend `GET /custom` (list) +
  `GET /stocks/{ticker}/custom/{slug}` (compute) → sandbox worker runs it → chart
  "Indicators ▾ → Custom" renders it (overlay/oscillator).
- **Infra CD**: platform repo auto-deploys on push to main (SSH → git pull +
  `docker compose` + caddy reload); GHCR login + `pull sandbox`. Secrets
  SSH_HOST/SSH_KEY set. JupyterLab + sandbox containers on the Hetzner VM
  (37.27.41.226); shared registry volume.

**Key components:** `tsp` SDK (engine: `Ctx`, indicators, `run_indicator`,
`publish`, `worker`), `Dockerfile.sandbox` + `build-sandbox` CI → GHCR image.

**Open decisions:** data free IEX vs paid SIP; public Jupyter URL vs SSH-tunnel
(currently tunnel-only); auth provider (Phase 4).

**Next:** Phase 2 (`+ Add` → name → opens seeded notebook; save/select) then
Phase 3 (notebook-left / chart-right layout). See `ROADMAP.md`.
